# Base 模块 SQL 说明

## 📌 重要提示

**本模块的 SQL 脚本已统一管理到项目根目录 `sql/` 文件夹中。**

## 📁 统一 SQL 文件位置

所有 SQL 脚本请使用项目根目录下的统一文件：

```
lumina/
└── sql/
    ├── 01_create_tables.sql      # 建表脚本（包含 Base 模块所有表）
    ├── 02_init_data.sql          # 初始化数据
    ├── 03_migration.sql          # 数据迁移脚本
    └── README.md                 # 详细使用说明
```

## 🚀 使用方法

### 首次安装

```bash
# 1. 执行建表脚本
mysql -u root -p lumina_dev < ../../../../sql/01_create_tables.sql

# 2. 执行初始化数据
mysql -u root -p lumina_dev < ../../../../sql/02_init_data.sql
```

### 从旧版本升级

```bash
# 1. 执行建表脚本
mysql -u root -p lumina_dev < ../../../../sql/01_create_tables.sql

# 2. 执行迁移脚本
mysql -u root -p lumina_dev < ../../../../sql/03_migration.sql
```

## 📊 Base 模块相关表

Base 模块包含以下数据库表：

- `lumina_user` - 用户表
- `lumina_tenant` - 租户表
- `lumina_role` - 角色表
- `lumina_permission` - 权限表
- `lumina_user_role` - 用户角色关联表
- `lumina_role_permission` - 角色权限关联表

详细说明请参考：`sql/README.md`

## ⚠️ 注意

- 本目录下的 SQL 文件已删除，统一使用根目录 `sql/` 下的脚本
- 如需模块独立部署，请参考根目录 `sql/` 中的脚本
- 所有表结构变更请更新根目录的 `01_create_tables.sql`

