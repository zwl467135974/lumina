# Agent 模块 SQL 说明

## 📌 重要提示

**Agent 模块的 SQL 脚本已统一管理到项目根目录 `sql/` 文件夹中。**

## 📁 统一 SQL 文件位置

所有 SQL 脚本请使用项目根目录下的统一文件：

```
lumina/
└── sql/
    ├── 01_create_tables.sql      # 建表脚本（包含 Agent 模块表）
    ├── 02_init_data.sql          # 初始化数据
    ├── 03_migration.sql          # 数据迁移脚本
    └── README.md                 # 详细使用说明
```

## 🚀 使用方法

### 首次安装

```bash
# 1. 执行建表脚本（包含 Agent 表）
mysql -u root -p lumina_dev < ../../../../sql/01_create_tables.sql

# 2. 执行初始化数据
mysql -u root -p lumina_dev < ../../../../sql/02_init_data.sql
```

### 从旧版本升级

```bash
# 1. 执行建表脚本
mysql -u root -p lumina_dev < ../../../../sql/01_create_tables.sql

# 2. 执行迁移脚本
mysql -u root -p lumina_dev < ../../../../sql/03_migration.sql
```

## 📊 Agent 模块相关表

Agent 模块包含以下数据库表：

- `lumina_agent` - Agent 表

**注意**：用户表（`lumina_user`）已迁移到 Base 模块，请使用 Base 模块的 SQL 脚本。

## 📝 模块独立部署（可选）

如果需要模块独立部署，可以使用 `agent.sql` 文件创建 Agent 表：

```bash
mysql -u root -p lumina_dev < src/main/resources/sql/agent.sql
```

**注意**：`agent.sql` 仅包含 Agent 表结构，不包含用户表。用户表请使用 Base 模块的 SQL 脚本。

## ⚠️ 注意

- 旧的 `user.sql` 文件已删除（已迁移到 Base 模块）
- 所有表结构变更请更新根目录的 `01_create_tables.sql`
- 详细说明请参考根目录：`sql/README.md`

