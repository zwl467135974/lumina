# Lumina 后端技术债务清单

> 审计日期：2026-07-09
> 范围：本 session 新增代码审查 + 端到端测试 + 认证流程审计
> 状态：待修复

---

## 一、本次新增代码审查问题

### LlmProvider 模块（lumina-business-agent）

#### 高严重度

| # | 问题 | 文件 | 说明 |
|---|------|------|------|
| 1 | 写操作缺 `@Transactional` | `LlmProviderServiceImpl.java` | `create()`/`update()`/`delete()` 无事务注解，Tenant 有 |
| 2 | `new ChatModelFactory()` | `LlmProviderServiceImpl.java:108` | ChatModelFactory 是 `@Component`，应注入而非 new |
| 3 | 硬编码 `tenantId = 0L` | `LlmProviderServiceImpl.java:73` | 绕过多租户隔离，应从 BaseContext 获取 |
| 4 | 异常用硬编码字符串 | `LlmProviderServiceImpl.java` (4处) | `"LLM Provider 不存在"` 等，应用 `ErrorCode` 枚举 |
| 5 | 缺少 Domain 层 | — | 无 `domain/model/LlmProvider.java`，Agent/Tenant 都有 Domain |

#### 中严重度

| # | 问题 | 说明 |
|---|------|------|
| 6 | Controller 缺 `@Slf4j` + `@Validated` | TenantController 有，导致请求无日志 + RequestParam 级校验不生效 |
| 7 | `@Audit` module 用大写 `"LLM_PROVIDER"` | 应为小写 `"llm_provider"`，与 Tenant 的 `"tenant"` 一致 |
| 8 | Create/Update 复用同一 DTO | Tenant 有独立的 `UpdateXxxDTO`，Update 时某些字段（如 provider）不可改 |
| 9 | QueryLlmProviderDTO 分页参数无校验 | 缺 `@Min`/`@Max`（Tenant 有 `@Min(1)`/`@Max(100)`） |
| 10 | 内联全限定类名 | `io.lumina.agent.model.AgentConfig.LLMConfig` 应 import |
| 11 | `getDecryptedApiKey(id)` 重复查询 | testConnection 里 selectById 后又调 getDecryptedApiKey 再查一次 |

### Dict 模块（lumina-business-base）

#### 高严重度

| # | 问题 | 文件 | 说明 |
|---|------|------|------|
| 12 | 异常用硬编码字符串 | `DictServiceImpl.java` (4处) | 应在 ErrorCode 加 `DICT_TYPE_NOT_FOUND`/`DICT_TYPE_ALREADY_EXISTS` 等 |

#### 中严重度

| # | 问题 | 说明 |
|---|------|------|
| 13 | Controller 缺 `@Slf4j` + `@Validated` | 同 LlmProvider |
| 14 | `@Audit` module 大写 `"DICT"` | 应为 `"dict"` |
| 15 | `@Audit` action 非标准 | `CREATE_TYPE`/`UPDATE_TYPE` 等非标准枚举，建议用 `CREATE`/`UPDATE`/`DELETE` |

### OnlineUser 模块（lumina-business-base）

#### 中严重度

| # | 问题 | 文件 | 说明 |
|---|------|------|------|
| 16 | Controller 缺 `@Slf4j` + `@Validated` | 同上 |
| 17 | `@Audit` module 大写 `"ONLINE_USER"` | 应为 `"online_user"` |
| 18 | `forceLogout` 用户不存在时静默返回 | 不报错不告警，应至少 `log.warn` |
| 19 | `Long.parseLong(parts[0])` 无异常保护 | Redis 数据格式损坏时 NumberFormatException |

---

## 二、跨模块共性问题

| # | 问题 | 影响范围 | 说明 |
|---|------|----------|------|
| 20 | 注入方式不统一 | 全部新代码 | 新代码用 `@RequiredArgsConstructor`，已有代码用 `@Autowired`。建议全项目统一（构造器注入更好，但需统一） |
| 21 | 缺类级 Javadoc | 多个文件 | 缺 `@author Lumina Team` / `@since 1.0.0` |
| 22 | VO 日期字段缺 `@JsonFormat` | 所有新 VO | 项目级问题，Tenant 也有。应加 `@JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")` |
| 23 | DTO `status` 字段缺范围校验 | LlmProvider/Dict | 应加 `@Min(0)`/`@Max(1)` |
| 24 | `LlmProviderDO` 部分字段缺 `@TableField` | — | TenantDO 每个字段都标注 |

---

## 三、端到端测试发现的问题

### 已修复 ✅

| # | 问题 | 修复 commit |
|---|------|-------------|
| 25 | Vue Flow Background SVG 拦截节点点击 | `99dc42b` |
| 26 | 模型管理 API 404（路径不匹配 Gateway 路由） | `0575378` |
| 27 | YAML branches 未解析为 edges | `99dc42b` |
| 28 | i18n 漏译 11 key（dashboard/login） | `99dc42b` |
| 29 | V23 Flyway 列名错误 | `d6d9ef3` |
| 30 | 在线用户 RedisTemplate 序列化不兼容 | `64a626c`（改用 RedisCacheManager） |
| 31 | Sidebar 缺少新页面菜单入口 | `04872f5`（V25） |
| 32 | `R.ok()` 方法名错误 | `2b82289`（改为 `R.success()`） |

### 待修复

| # | 问题 | 说明 |
|---|------|------|
| 33 | `login.footerHint` i18n 漏译 | 已在 `99dc42b` 修复，但可能还有其他漏译 key 未发现 |

---

## 四、认证/安全层面的架构问题

> 来源：认证流程审计（explore agent 报告）

| # | 问题 | 严重度 | 说明 | 状态 |
|---|------|--------|------|------|
| 34 | **Token 无法吊销** | 高 | logout 只清前端，已签发的 token 在过期前（7天）始终有效 | ✅ 已修复：logout 接入 `addTokenToBlacklist` |
| 35 | **Token 黑名单已实现但未接入** | 高 | `RedisCacheManager.addTokenToBlacklist/isTokenBlacklisted` 是现成的，但 Gateway filter 和 AuthController.logout 都没调用 | ✅ 已修复：Gateway P0 已接入，logout 本次接入 |
| 36 | **权限快照陈旧** | 高 | permissions 全量打进 JWT，权限回收最迟 Token 有效期满才反映到网关 | ⚠️ 最小修复：默认 Token 有效期缩短至 2 小时（`lumina.jwt.expiration` 默认 7200000ms）。完整修复需改为 Gateway filter 实时查 Redis 权限缓存，后续迭代处理 |
| 37 | **两套 AuthController 不一致** | 中 | base 模块用 `roles/permissions/tenantId`，agent 模块用单数 `role`，parseTokenToLoginUser 期望复数 → agent 签发的 token 解析出来 roles/permissions 是 null | 待修复 |
| 38 | **信任下游 Header** | 中 | Gateway 把用户身份通过 `X-User-Id` header 透传，需确保下游服务只接受来自 Gateway 的请求 | 待修复 |
| 39 | **logout 未加 token 黑名单** | 中 | agent/base AuthController.logout 未调用 `addTokenToBlacklist` | ✅ 已修复：两处 logout 均加入黑名单 |
| 40 | **LoginVO.expiration 硬编码** | 低 | 写死 `now + 7天`，未读取实际 JWT 过期时间 | ✅ 已修复：改为 `jwtUtil.getExpiration(token).getTime()`，与 JWT 实际过期一致 |

---

## 五、根因分析

### 为什么会犯这些错？

| 根因 | 具体表现 | 防范措施 |
|------|----------|----------|
| **没加载后端技能包** | 写 DictController 直接注入 Mapper、没遵循分层 | 后端 Pre-Delivery Checklist 要求"写代码前 load skill" |
| **没先读已有代码** | V23 列名错、R.ok() 方法名错、路径不匹配 Gateway | Checklist 要求"读一个同类已有模块作为模板" |
| **急于求成** | DictController 偷懒跳过 Service、LlmProvider 跳过 Domain | 无捷径，分层是底线 |
| **没有自检** | 编译通过就提交，没有检查 @Transactional/ErrorCode/Audit 规范 | Checklist 14 项强制自检 |

### 已补建的防范措施

| 措施 | 文件 | 状态 |
|------|------|------|
| 后端 Pre-Delivery Checklist（14 项） | `AGENTS.md` | ✅ 已加 |
| lumina_redis 技能包 | `skills/lumina_redis/SKILL.md` | ✅ 已建 |
| lumina_flyway 技能包 | `skills/lumina_flyway/SKILL.md` | ✅ 已建 |

---

## 六、修复优先级

### P0 — 安全/数据完整性（必须）

| # | 问题 | 预估工作量 |
|---|------|-----------|
| 34 | Token 无法吊销（接入黑名单到 Gateway + logout） | 中 |
| 35 | Token 黑名单接入 | 小（已有代码，只需调用） |
| 1 | LlmProviderServiceImpl 写操作加 @Transactional | 小 |
| 2 | new ChatModelFactory() 改为注入 | 小 |
| 4+12 | 异常用 ErrorCode 枚举（LlmProvider + Dict） | 中 |

### P1 — 架构合规（重要）

| # | 问题 | 预估工作量 |
|---|------|-----------|
| 3 | tenantId 从 BaseContext 获取 | 小 |
| 5 | LlmProvider Domain 层 | 中 |
| 37 | 两套 AuthController 统一 | 中 |
| 36 | 权限不塞 JWT，改为实时查询 | 大 |
| 6+13+16 | 3 个 Controller 加 @Slf4j + @Validated | 小 |
| 7+14+17 | 3 个 Controller @Audit module 改小写 | 小 |

### P2 — 风格统一（后续）

| # | 问题 | 预估工作量 |
|---|------|-----------|
| 20 | 注入方式统一 | 中（全项目） |
| 21 | Javadoc 补全 | 小 |
| 22 | @JsonFormat 统一 | 小（全项目） |
| 8 | UpdateLlmProviderDTO 拆分 | 小 |
| 9 | QueryDTO 分页参数校验 | 小 |
| 10 | import 全限定类名清理 | 小 |
| 11 | 重复查询优化 | 小 |
| 18+19 | OnlineUser 健壮性 | 小 |
| 23+24 | DTO/DO 注解完善 | 小 |

---

## 附录：本次 session 新增的后端文件清单

| 模块 | 文件 | 路径 |
|------|------|------|
| 加密工具 | CryptoUtil.java | lumina-common |
| LlmProvider | 7 文件 | lumina-business-agent |
| Dict | 10 文件 | lumina-business-base |
| OnlineUser | 4 文件 | lumina-business-base |
| RedisCacheManager 扩展 | ZSET 方法 | lumina-framework |
| Flyway | V23/V24/V25 | lumina-business-base |
| 技能包 | lumina_redis/lumina_flyway | skills/ |

---

## 七、已有代码全面审计（97 项）

> 审计日期：2026-07-09
> 范围：全量后端代码（gateway / framework / common / business-base / business-agent / agent-core）
> 方式：4 个 agent 并行审计，逐文件源码审查

### 审计总览

| 模块 | 高 | 中 | 低 | 合计 |
|------|---|---|---|------|
| Gateway | 3 | 4 | 3 | **10** |
| Framework | 2 | 4 | 4 | **10** |
| Common | 2 | 2 | 3 | **7** |
| Business-Base（已有） | 8 | 5 | 8 | **21** |
| Business-Agent（已有） | 9 | 13 | 6 | **28** |
| Agent-Core | 4 | 8 | 9 | **21** |
| **合计** | **28** | **36** | **33** | **97** |

### 关键发现（Top 10 最紧急）

| 排名 | 问题 | 严重度 | 模块 |
|------|------|--------|------|
| 1 | **网关 Header 注入越权** — 客户端可伪造 X-User-Id 绕过认证 | 致命 | Gateway |
| 2 | **白名单 startsWith 前缀绕过** — /api/login 匹配 /api/loginInfo | 致命 | Gateway |
| 3 | **SpEL 表达式注入 RCE** — 工作流表达式可执行任意代码 | 致命 | Agent-Core |
| 4 | **并行工作流 HashMap 竞态** — 并发写共享 ctx 导致数据损坏/死循环 | 致命 | Agent-Core |
| 5 | **RoleController/PermissionController/TenantController 全接口无权限** | 致命 | Business-Base |
| 6 | **会话/工作流/Prompt/知识库/预算的租户隔离全部失效（IDOR）** | 致命 | Business-Agent |
| 7 | **循环工作流完全失效** — LoopSignal 从未被引擎处理 | 功能 | Agent-Core |
| 8 | **并行工作流后必停** — executeParallelBranches 永远返回 null | 功能 | Agent-Core |
| 9 | **Token 黑名单已实现但从未调用** — logout 后 Token 仍有效 | 安全 | Gateway |
| 10 | **密码明文写入日志** — Feign Fallback 打印 CreateUserDTO | 安全 | Business-Base |

---

### 7.1 Gateway 模块（10 项）

#### 高严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| G1 | **Header 注入越权** | `JwtAuthFilter:74-80` | `mutate().header()` 是追加语义，不删除客户端原有 Header。攻击者可注入 `X-User-Id:1; X-Roles:SUPER_ADMIN` |
| G2 | **白名单 startsWith 前缀绕过** | `WhitelistConfig:44` | `/api/login` 匹配 `/api/loginInfo`。应改为 `path.equals(wp) \|\| path.startsWith(wp + "/")` |
| G3 | **Token 黑名单未检查** | `JwtAuthFilter:62-68` | `RedisCacheManager.isTokenBlacklisted()` 已实现但网关从未调用 | ✅ P0 已修复 |

#### 中严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| G4 | **CORS 通配符 + 凭证** | `GatewayConfig:30-31` | `allowedOriginPatterns("*")` + `allowCredentials(true)`，生产环境凭证泄漏 |
| G5 | **限流信任 X-Forwarded-For** | `RateLimitFilter:94` | 攻击者每次请求换 IP 头即绕过限流 |
| G6 | **限流 ipCounts Map 无上限** | `RateLimitFilter:49,70` | 随不同 IP 无限增长，OOM 风险 |
| G7 | **缺 WebFlux 全局异常处理** | 整个模块 | `@RestControllerAdvice`（MVC）对 WebFlux 无效，路由失败暴露原始错误 |

#### 低严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| G8 | **限流响应体格式不统一** | `RateLimitFilter:115-118` | 硬编码 `{"code":429}`，与 `R<>` 格式不一致 |
| G9 | **字段注入而非构造器注入** | `JwtAuthFilter:29-33` | 用 `@Autowired`，应改 `@RequiredArgsConstructor` |
| G10 | **getClientIp 运算符优先级 Bug** | `RateLimitFilter:98` | `\|\|` 和 `&&` 优先级导致逻辑错误 |

---

### 7.2 Framework 模块（10 项）

#### 高严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| F1 | **FileService 抛 RuntimeException 而非 ErrorCode** | `FileService:47,58,95,107` | 全局异常处理器归类为 500「系统异常」，丢失文件具体错误 |
| F2 | **FileService 写操作缺 @Transactional** | `FileService:59,102` | DB 操作与文件存储不一致，孤立文件风险 |

#### 中严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| F3 | **RedisConfig ObjectMapper 与全局不一致** | `RedisConfig:106-110` | Redis 中日期格式与 HTTP 接口不一致，反序列化可能失败 |
| F4 | **GenericJackson2JsonRedisSerializer 默认类型信息** | `RedisConfig:88,110` | `@class` 嵌入有反序列化攻击风险 |
| F5 | **WebMvcConfig CORS 通配符** | `WebMvcConfig:23-28` | 同 G4，对所有 MVC 服务生效 |
| F6 | **JacksonConfig 无条件 INDENT_OUTPUT** | `JacksonConfig:99` | 生产环境响应体增 10-30% 字节，无 Profile 条件判断 |

#### 低严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| F7 | **FileMapper 包路径不符合 @MapperScan** | `FileMapper:13` | `storage.mapper` 不匹配 `io.lumina.*.infrastructure.mapper` |
| F8 | **FileService.md5Hex 吞没异常** | `FileService:137-149` | `catch { return null; }` 无日志 |
| F9 | **MinioStorageClient 抛 RuntimeException** | `MinioStorageClient:53,69,81` | 应用 SystemException |
| F10 | **AuditAspect 依赖参数名猜测 targetId** | `AuditAspect:98-128` | 启发式提取，应支持 `@Audit(targetIdParam=)` |

---

### 7.3 Common 模块（7 项）

#### 高严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| C1 | **JwtUtil 硬编码默认密钥** | `JwtUtil:31` | 无运行时检查，生产遗漏配置即等于无加密 |
| C2 | **refreshToken 复用旧 Claims** | `JwtUtil:149-154` | 旧权限/旧时间戳带入新 Token，权限降级不生效 |

#### 中严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| C3 | **CryptoUtil 默认密钥无生产保护** | `CryptoUtil:27-30` | 同 C1，无运行时校验 |
| C4 | **BaseContext.initFromHeaders 异常吞没** | `BaseContext:264-277` | `Long.valueOf("abc")` 被吞没，tenantId 保持 null → 可能跨租户 |

#### 低严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| C5 | **LoginContext record 持有可变数组** | `LoginContext:12-18` | `String[]` 可被外部修改 |
| C6 | **PageResult pageSize=0 除零** | `PageResult:55` | `Math.ceil(total/0)` = Infinity → cast 0 |
| C7 | **StringUtil extends StringUtils** | `StringUtil:13` | 继承工具类反模式；DateUtil/CollectionUtil 缺私有构造器 |

---

### 7.4 Business-Base 已有代码（21 项）

#### 高严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| B1 | **RoleController 全接口无 @RequirePermission** | `RoleController:30-124` | 任何已认证用户可创建/修改/删除角色、分配权限 → **权限提升** |
| B2 | **PermissionController 全接口无 @RequirePermission** | `PermissionController:27-95` | 可破坏整个权限体系 |
| B3 | **TenantController 无权限 + 跳过租户隔离** | `TenantController:28-86` | `lumina_tenant` 在 IGNORE_TABLES，无权限注解 → 灾难性漏洞 |
| B4 | **AuditLogController 无权限 + 直接注入 Mapper** | `AuditLogController:29-74` | 返回 DO 暴露表结构，任何用户可查所有审计日志 |
| B5 | **MenuController 直接注入 PermissionMapper** | `MenuController:38-39` | 跳过 Service 层 |
| B6 | **getUserByUsername 无权限** | `UserController:131-143` | 同租户信息泄露 |
| B7 | **FeignFallback 日志打印明文密码** | `BaseFeignClientFallback:20` | `log.error("dto={}", dto)` 打印含 password 的 DTO |
| B8 | **logout 空操作** | `AuthServiceImpl:132-138` | Token 7 天内始终有效 | ✅ 已修复：logout 加入 Token 黑名单 + 清除在线记录 |

#### 中严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| B9 | **Token 过期硬编码 7 天** | `AuthServiceImpl:124` | 应走配置，建议缩短至 2-4h + Refresh Token | ✅ 已修复：默认缩短至 2h（`lumina.jwt.expiration` 默认 7200000ms），expiration 改为从实际 JWT 解析 |
| B10 | **assignPermissions 未校验权限 ID** | `RoleServiceImpl:278-283` | 可插入大量不存在的权限 ID |
| B11 | **updateTenant 缺 @Transactional** | `TenantServiceImpl:131` | `createTenant` 和 `deleteTenant` 有 |
| B12 | **deleteTenant NPE 风险** | `TenantServiceImpl:151-152` | `selectById` 返回 null 时直接调 setter |
| B13 | **Permission/Tenant/Role 缺少 Domain 层** | — | Domain Entity 存在但 ServiceImpl 全部直接操作 DO |

#### 低严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| B14 | **全部 Controller @Autowired 字段注入** | 7 个 Controller | 应统一 `@RequiredArgsConstructor` |
| B15 | **批量关联逐条 insert（N+1 写入）** | `UserServiceImpl:83-88` 等 3 处 | 应用 `saveBatch()` |
| B16 | **软删除方式不统一** | 4 处 | User 用 `deleteById`，Role/Permission/Tenant 手动 `setDeleted(1)` |
| B17 | **PermissionServiceImpl 手动追加 deleted 条件** | `PermissionServiceImpl:127-131` | `@TableLogic` 已自动追加，冗余 |
| B18 | **RoleController 硬编码 size=100** | `RoleController:89` | 超过 100 静默丢弃 |
| B19 | **LoginDTO 未用 Lombok** | `LoginDTO:12-56` | 手动 getter/setter，风格不一致 |
| B20 | **UserServiceImpl 死 import** | `UserServiceImpl:21,11,31,32` | 4 个未使用 import |
| B21 | **3 个 Controller 缺 @Tag Swagger 注解** | Role/Permission/Tenant | 接口文档无分组 |

---

### 7.5 Business-Agent 已有代码（28 项）

#### 高严重度 — 租户隔离系统性失效（IDOR）

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| A1 | **会话跨租户越权** | `ConversationServiceImpl:88-96` | `getByUuid()` 不校验 tenantId → 读/删/写他人对话 |
| A2 | **流式多模态执行绕过租户校验** | `AgentServiceImpl:427-430` | `selectById` 替代 `getAgentById` → 消耗他人 LLM 配额 |
| A3 | **工作流定义/实例/日志跨租户** | `WorkflowServiceImpl:134-140` | `getById` 不校验 → 修改/删除/执行他人工作流 |
| A4 | **Prompt 跨租户** | `PromptServiceImpl:200-206` | `getById` 不校验 → 修改/删除/发布他人模板 |
| A5 | **知识库文档跨租户删除** | `KnowledgeServiceImpl:190-197,225-235` | 不校验即删 DB + 向量数据 |
| A6 | **预算规则跨租户删除** | `BudgetServiceImpl:104-113` | 不校验 tenantId |
| A7 | **知识库挂载/卸载跨租户** | `KnowledgeBaseServiceImpl:86-109` | 不校验 agentId 归属 |
| A8 | **评估可执行他人 Agent** | `EvaluationServiceImpl:160-161,194-195` | 消耗他人 LLM 资源 |
| A9 | **编排引擎桥接无租户校验** | `AgentExecutionHandlerBridge:33-39` | `selectById` 无 tenantId |

#### 中严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| A10 | **向量检索无租户过滤** | `KnowledgeServiceImpl:259-278` | 返回全局向量结果 |
| A11 | **SSE 任务进度流无校验** | `AgentTaskServiceImpl:335-343` | sink 存在时直接返回 |
| A12 | **登录无防暴力破解** | `UserServiceImpl:60-111` | 无失败次数限制/验证码/IP 限流 |
| A13 | **AgentRateLimiter 直接用 RedissonClient** | `AgentRateLimiter:36,44-47` | 违反 lumina_redis 规范 |
| A14 | **Controller 返回 DO 而非 VO** | 7 个 Controller | 泄露 deleted/tenantId 等内部字段 |
| A15 | **@Transactional 缺 rollbackFor** | `WorkflowServiceImpl:58,80`/`PromptServiceImpl` | checked Exception 不回滚 |
| A16 | **写操作完全缺 @Transactional** | Workflow/Prompt/Budget/Task | 7 个方法含 DB 写无事务 |
| A17 | **硬编码异常字符串** | 4 处 | `RuntimeException("Agent 不存在")` 等 |
| A18 | **@Audit 注解普遍缺失** | 7 个 Controller | 无法追溯 |
| A19 | **Controller 注入方式不统一** | 6 个 @Autowired vs 5 个 @RequiredArgsConstructor | — |
| A20 | **WorkflowController 分页不规范** | `WorkflowController:60-66,107-114` | 返回 List 而非 PageResult |
| A21 | **成本计算硬编码 "default" 模型** | `CostServiceImpl:121`/`BudgetServiceImpl:179` | 忽略实际 provider/model，成本失真 |
| A22 | **流式响应无背压/超时** | `AgentServiceImpl:397-415,474-493` | 无 `.timeout()`，无 `doOnError`/`doOnCancel` |

#### 低严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| A23 | **领域模型抛 JDK 异常** | `Agent.java:74,84,101-113` | 应抛 BusinessException(ErrorCode) |
| A24 | **AgentVO 日期无 @JsonFormat** | `AgentVO:57,62` | — |
| A25 | **executeAgentMultimodal 代码重复** | `AgentServiceImpl:284-360 vs 418-494` | — |
| A26 | **异步上下文重建不统一** | Workflow/Evaluation/Task 各自实现 | — |
| A27 | **流式 StringBuilder 非线程安全** | `AgentServiceImpl:395,472` | — |
| A28 | **流中断时消息不持久化** | `DefaultAgentExecutionEngine:249-254` | `doOnComplete` 在 cancel 时不触发 |

---

### 7.6 Agent-Core 模块（21 项）

#### 高严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| E1 | **SpEL 表达式注入 RCE** | `SpelExpressionEvaluator:31-36` | 默认 `StandardEvaluationContext` 允许 `T(Runtime).exec()` |
| E2 | **并行工作流共享 HashMap 竞态** | `DefaultWorkflowEngine:204-219` | `WorkflowContext` 的 maps 非线程安全，并发写导致数据损坏/死循环 |
| E3 | **LoopSignal 从未被引擎处理** | `DefaultWorkflowEngine:158-162` | 只检查 ParallelSignal 不检查 LoopSignal → **循环节点完全失效** |
| E4 | **executeParallelBranches 永远返回 null** | `DefaultWorkflowEngine:243-253` | **并行节点后工作流必停** |

#### 中严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| E5 | **Prompt 注入无防御** | `PromptLoader:55-63` + `DefaultAgentExecutionEngine:147` | 原始用户输入直接 replace 进系统提示 |
| E6 | **工具执行无沙箱** | `EnhancedToolManager:76-80` | LLM 可调用任意 @AgentTool 方法，默认白名单 null = 全部 |
| E7 | **MemoryManager 直接用 RedisTemplate** | `MemoryManager:43-44` | 违反 lumina_redis 规范 |
| E8 | **MemoryManager 内存降级存储无淘汰** | `MemoryManager:38,77-86` | `memoryStore` ConcurrentHashMap 无全局上限/过期 |
| E9 | **MemoryManager Redis 读-改-写竞态** | `MemoryManager:92-121` | 先读列表→追加→写回，并发消息丢失 |
| E10 | **ToolCircuitBreaker 状态转换竞态** | `ToolCircuitBreaker:47-86` | check-then-act 不原子 |
| E11 | **ToolInvocationRecorder max/min 非原子** | `ToolInvocationRecorder:131-148` | volatile 不保证复合操作 |
| E12 | **WorkflowDefinition 图操作 O(N²)** | `WorkflowDefinition:72-107` | 每步 stream().filter()，应预建邻接表 |

#### 低严重度

| # | 问题 | 文件:行 | 描述 |
|---|------|---------|------|
| E13 | **DefaultAgentExecutionEngine 混用注入** | `:61-107` | 4 构造器 + 8 字段注入 |
| E14 | **每次执行重建 Agent/Toolkit/Model** | `:441-478` | 连接池/Schema 不可复用 |
| E15 | **多处 new ObjectMapper()** | 6 处 | 应注入全局实例 |
| E16 | **ConfigLoader 缓存永不过期** | `ConfigLoader:34,72-92` | 未注册 Nacos listener |
| E17 | **LlmResilienceWrapper 重试参数硬编码** | `LlmResilienceWrapper:34-39` | 3 次/500ms 无指数退避 |
| E18 | **流式记忆保存不可靠** | `DefaultAgentExecutionEngine:249-254` | cancel 时 doOnComplete 不触发 |
| E19 | **RAG JSON 手动拼接** | `DefaultAgentExecutionEngine:537-567` | escapeJson 不处理 Unicode 控制字符 |
| E20 | **ToolDefinitionAdapter conversationId 始终 null** | `:144-163` | 工具调用记录无法关联会话 |
| E21 | **EnhancedToolManager 参数解析失败静默返回空** | `:247-296` | 工具以 null 参数"成功"执行 |

---

## 八、修订后的全量修复优先级

### P0 — 安全漏洞（阻断级，立即修复）

| # | 问题 | 模块 | 工作量 |
|---|------|------|--------|
| G1 | Header 注入越权 | Gateway | 小 |
| G2 | 白名单 startsWith 绕过 | Gateway | 小 |
| G3 | Token 黑名单未接入 | Gateway | 小 |
| E1 | SpEL 表达式注入 RCE | Agent-Core | 中 |
| E2 | 并行工作流 HashMap 竞态 | Agent-Core | 中 |
| B1-B3 | Role/Permission/TenantController 无权限 | Business-Base | 小 |
| A1-A9 | 租户隔离系统性失效（IDOR） | Business-Agent | 中 |
| B7 | 密码明文写入日志 | Business-Base | 小 |
| C1 | JwtUtil 默认密钥无保护 | Common | 小 |
| C3 | CryptoUtil 默认密钥无保护 | Common | 小 |
| 34-35 | Token 无法吊销 + 黑名单未接入（原清单） | 全局 | 中 |

### P1 — 功能缺陷 + 架构合规（重要）

| # | 问题 | 模块 | 工作量 |
|---|------|------|--------|
| E3 | 循环工作流完全失效 | Agent-Core | 中 |
| E4 | 并行节点后工作流必停 | Agent-Core | 中 |
| G7 | 网关缺 WebFlux 异常处理 | Gateway | 中 |
| B4-B5 | AuditLog/MenuController 跳过 Service 层 | Business-Base | 中 |
| A14 | Controller 返回 DO 而非 VO | Business-Agent | 中 |
| A15-A16 | @Transactional 缺失/不完整 | Business-Agent | 小 |
| E7 | MemoryManager 直接用 RedisTemplate | Agent-Core | 中 |
| A13 | AgentRateLimiter 直接用 RedissonClient | Business-Agent | 小 |
| F1-F2 | FileService 异常+事务 | Framework | 小 |
| 37 | 两套 AuthController 不一致（原清单） | 全局 | 中 |

### P2 — 性能 + 代码质量（后续迭代）

| 范围 | 问题数 | 典型 |
|------|--------|------|
| 注入方式统一 | ~15 | @Autowired → @RequiredArgsConstructor |
| @Audit 补全 | ~20 | 7 个 Controller 无 @Audit |
| 软删除统一 | 4 | deleteById vs setDeleted(1) |
| N+1/批量写入 | 3 | saveBatch 替代循环 insert |
| ObjectMapper 复用 | 6 | new ObjectMapper() → @Autowired |
| 代码重复/清理 | ~10 | 死 import / 手动 JSON / 私有构造器 |

---

## 附录：审计方法

| 步骤 | 工具 | 说明 |
|------|------|------|
| 1 | glob + grep | 扫描全量 Java 文件，按模块/类型分类 |
| 2 | Read | 逐文件阅读源码（约 150 个文件） |
| 3 | 对比分析 | 与正确实现的同类模块对比（如 Tenant vs LlmProvider） |
| 4 | 安全维度 | OWASP Top 10 逐项检查 |
| 5 | 4 agent 并行 | Gateway+Framework+Common / Business-Base / Business-Agent / Agent-Core |
