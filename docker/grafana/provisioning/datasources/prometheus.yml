# Grafana 数据源自动配置
# 启动时自动创建 Prometheus 数据源，无需手动在 UI 添加
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: '15s'
